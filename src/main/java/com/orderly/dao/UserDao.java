package com.orderly.dao;

public class UserDao {

	/**
	 * Not sure if this is needed if we're using Firebase. 
	 * May be a good idea to have this class called to handle. 
	 * adding, removing, updating, etc of users in Firebase.
	 */
}
